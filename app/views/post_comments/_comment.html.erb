<%= form_with(model:[@post, @post_comment])do |f| %>
  <%= f.text_area :comment ,placeholder: "コメントをここに",size: "30", class: "form-control"%>
  <%= f.submit "送信" ,class: "btn btn-secondary" %>
<% end %>