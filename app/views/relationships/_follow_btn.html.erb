<% unless user == current_user %>
  <% if current_user.following?(user) %>
    <%= link_to "フォローを外す", user_relationship_path(user.id follow.id), method: :delete %>
  <% else %>
    <%= link_to "フォローをする", user_relationships_path(user.id), method: :post %>
  <% end %>
<% end %>