<% unless user == current_user %>
  <% if current_user.following?(user) %>
    <%= link_to "フォローを外す", relationship_path(user.id), method: :delete %>
  <% else %>
    <%= link_to "フォローをする", relationships_path(user_id: user.id), method: :post %>
    <%#= form_with url: relationships_path, local: true do |f| %>
      <%#= hidden_field_tag :user_id, user.id %>
      <%#= hidden_field_tag :user_id, user.id %>
      <%#= hidden_field_tag :user_id, user.id %>
      <%#= hidden_field_tag :user_id, user.id %>
      <%#= f.submit 'フォローをする', class: 'btn' %>
    <%# end %>
  <% end %>
<% end %>