<%= link_to notifications_path do %>
    <% if user_signed_in? && current_user.unchecked_notifications.any? %>

    <% else %>
        <i class="fas fa-bell fa-lg nav-link text-dark" style="font-size: 1.3em;"></i>
    <% end %>
<% end %>
