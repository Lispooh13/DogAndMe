<%= link_to notifications_path do %>
    <% if user_signed_in? && current_user.unchecked_notifications.any? %>
        <i class="fas fa-bell bell-mark-circle"></i>
        <i class="fas fa-circle n-circle"></i>
    <% else %>
        <i class="fas fa-bell bell-mark"></i>
    <% end %>
<% end %>
