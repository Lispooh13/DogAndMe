<h1>トップページ</h1>

<%= form_with(url: search_path, local: true, method: :get, class: "search-form") do |f| %>
  <%= f.text_field :keyword, placeholder: "キーワードを入力",size: "50", class: "search-input" %>
  <%= f.submit "検索",class: "search-btn" %>
<% end %>